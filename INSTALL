Installing NetPanzer Map Editor
================================

See BUILD.md for full build, test, and AppImage instructions.

Quick start:

  $ meson setup build
  $ ninja -C build
  $ NETPANZER_DATADIR=/path/to/netpanzer/data ./build/netpanzer-editor

The editor runs directly from the build directory; no install step is required.

# EOF #
