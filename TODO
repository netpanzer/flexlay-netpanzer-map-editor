Known missing features
======================

- Tileset theme switching UI (Desert / IcyWinter / Martian / etc. variants
  alongside the default summer12mb)

- Multi-buffer / tabbed map editing

- Object count validation (enforce max outposts and spawn points per the
  netPanzer map specification)

# EOF #
